# Bash

Use OpenCode's built-in `bash` tool for shell commands.

Do not call a skill named Bash.
Do not output JSON tool calls.
When you need to run a command, use the built-in `bash` tool.
When you need to create or modify files, use the built-in `write` or `edit` tools.